apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: mongodb
  namespace: mongodb
spec:
  hosts:
    - '*'
  gateways:
  - mongodb
  tcp:
  - match:
    - port: 8080
    route:
    - destination:
        host: mongodb
        port:
          number: 27017
 
